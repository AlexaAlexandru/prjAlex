using System;
using System.ComponentModel.DataAnnotations;
using System.Reflection.Metadata.Ecma335;
using SchedulePlatform.Models.Entities;
using SchedulePlatform.Models.Enums;

namespace SchedulePlatform.WebUI.Pages
{
    public partial class Appointment
    {
        //    private List<DateTime> appointments = new List<DateTime>();

        //    public DateTime StartDate { get; set; } = DateTime.Parse("09:00");
        //    public DateTime EndDate { get; set; }= DateTime.Parse("17:00");
        //    public Boolean IsOnSite { get; set; }
        //    public TimeSpan Duration { get; set; } = new TimeSpan(0, 30, 0);
        //    public TypeAppointment Type { get; set; }

        //    public void DislaySlots()
        //    {
        //        while (StartDate>EndDate)
        //        {
        //            appointments.Add(StartDate);
        //            StartDate += Duration;
        //        }
        //    }
    }

    public class TimeSlots
    {
        //public DateTime StartTime { get; set; } = DateTime.Parse("09:00");
        //public DateTime EndTime { get; set; } = DateTime.Parse("17.00");
        //public TimeSpan Duration { get; set; } = new TimeSpan(0, 30, 0);

        //private List<DateTime> appointments = new List<DateTime>();

        //public void DisplaySlots()
        //{
        //    while (StartTime<EndTime)
        //    {
        //        appointments.Add(StartTime);
        //        StartTime += Duration;
        //    }
        //}
    }


}